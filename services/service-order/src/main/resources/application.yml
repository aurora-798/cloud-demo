spring:
  application:
    name: server-order
  config:
    import: nacos:server-order.yml
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
server:
  port: 8000


logging:
  level:
    com.shuhang.feign: debug